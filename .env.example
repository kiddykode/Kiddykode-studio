# URL of the API server — used by the frontend to make API and auth requests.
# In development this points to the local Hono server.
# In production on Vercel, leave empty so requests go to the same origin via rewrites.
VITE_API_URL=http://localhost:3001
